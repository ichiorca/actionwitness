{
  "name": "actionwitness-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Minimal React workspace for the assurance harness (spec v1.8 §8.4, §19.1). The WebMCP hook package is intentionally NOT pinned yet: run the §25.1 spike (use-webmcp-tool vs usewebmcp) against the exact tested Chrome build, then add exactly one and record the decision in the README pinning table.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@testing-library/react": "^16.1.0",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "jsdom": "^25.0.1",
    "typescript": "^5.7.2",
    "vite": "^6.0.3",
    "vitest": "^2.1.8",
    "webmcp-types": "*"
  }
}
