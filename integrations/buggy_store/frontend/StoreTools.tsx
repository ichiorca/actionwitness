/**
 * Harness-facing WebMCP bridge for the five demo-store tools (spec §11.2,
 * Appendix D.2): search_catalog, get_cart, update_cart, apply_discount,
 * proceed_to_checkout — registered via the harness adapter, enabled only while
 * a contract is armed/running, dispatched through the harness invocation
 * endpoint so the lifecycle is recorded (§15.5).
 *
 * Scaffolding only — implemented in Tier 1 after the §25.1 hook spike.
 */
export {};
