"""`store_state` ObservationProvider for the Buggy Store (scaffolding only).

Canonical server-side payload mounted under the `store.` namespace (§9.3);
provider state_version stays metadata, never business payload.
"""
