"""ManagedTargetAdapter implementation for the Buggy Store (scaffolding only).

Must publish the deterministic tool-effect map (§13.4) and reach the store ONLY
through its versioned /demo/api/v1 surface — no direct service imports (LD-8).
"""
