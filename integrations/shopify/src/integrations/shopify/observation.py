"""`shopify_cart_state` ObservationProvider (scaffolding only).

Locale-aware same-session GET /cart.js, JSON <= 256 KiB, provenance
`platform_session_api`; never derived from tool-return or DOM text (FR-112, FR-117).
"""
