# Validate the cavesson manifest

Run `cavesson lint` (add `--strict` when the operator asks for the tightest gate) with your shell tool. Report whether the manifest is valid and surface every error/warning with the file + rule and how to fix it. Read-only — it never writes.
