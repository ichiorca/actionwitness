# Mine cavesson instincts

Run `cavesson learn` with your shell tool to distill the L1 event log into instinct-style memory entries. Report what was learned. High-confidence instincts surface in AGENTS.md next session, so review them for correctness before relying on them.
