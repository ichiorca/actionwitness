# Cavesson readiness check

Run `cavesson doctor` (and `cavesson spec-kit doctor` if this is a spec-kit project) with your shell tool. Report the readiness summary and surface every `[WARN]`/`[FAIL]` with what it means and how to fix it. Use this before starting a build or after changing configuration.
