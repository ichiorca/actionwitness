# Show cavesson depth

Run `cavesson levels status` with your shell tool and report the current depth ladder (L0–L4) and which standard_l* flags are enabled. If the operator asks to change depth, use `cavesson levels set <L2|L3|L4>` (cumulative) — but confirm first, since it recompiles.
