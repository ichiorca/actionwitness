# Report cavesson cost

Run `cavesson cost --window-days 1` (adjust the window if the operator asks) with your shell tool and report the spend, broken down by model/session as available. Note if it approaches the configured cost cap.
