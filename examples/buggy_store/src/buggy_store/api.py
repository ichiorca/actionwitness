"""Buggy Store versioned HTTP API (/demo/api/v1 when composed — spec §15.5).

Scaffolding only: endpoints, workspace scoping, and seeding land in Tier 1.
"""


def main() -> int:
    """Console entry point for the standalone store (`buggy-store`)."""
    print("buggy-store: scaffolded but not implemented yet (Tier 1, spec §15.5).")
    return 2
